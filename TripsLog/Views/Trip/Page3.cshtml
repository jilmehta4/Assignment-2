@model Trip

@{
    //string dest = @Model.Destination;
    //ViewBag.Title = "Add Things To Do in" + dest;
}

<form asp-controller="Trip" asp-action="Create" method="post">
    <div class="form-group">
        <div>
            <input type="text" value="Thing 1"asp-for="ThingToDo1" class="form-control" />
        </div>
    </div>
    <div class="form-group">

        <div>
            <input type="text" value="Thing 2" asp-for="ThingToDo2" class="form-control" />
        </div>
    </div>
    <div class="form-group">

        <div>
            <input type="text" value="Thing 3" asp-for="ThingToDo3" class="form-control" />
        </div>
    </div>
    <div class="row pt-2 btn-group">
        <div class="col-sm-4">
            <button type="submit" class="btn btn-secondary">Save</button>
        </div>
        <div class="col-sm-4">
            <a asp-action="Index" asp-controller="Home" class="btn btn-outline-secondary">Cancel</a>
        </div>
    </div>
</form>